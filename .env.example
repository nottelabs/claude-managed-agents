# Drives Claude Managed Agents (environments, vaults, agents, sessions).
# Get one at https://platform.claude.com/
ANTHROPIC_API_KEY=

# Injected into the sandbox as an environment_variable vault credential.
# Get one at https://console.notte.cc/apikeys
NOTTE_API_KEY=

# Optional: per-operator wall-clock timebox in minutes (default 5).
# OPERATOR_TIMEBOX_MIN=5
